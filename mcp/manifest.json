{
  "manifest_version": "0.2",
  "name": "oatpad",
  "display_name": "Oatpad",
  "version": "1.0.1",
  "description": "Search Oatpad meetings by title, fetch one by id, pull every meeting in a date range, or schedule a new meeting for a future slot.",
  "long_description": "Bridges Claude Desktop to the MCP server running inside the Oatpad macOS app. The bundle is a thin proxy: it forwards JSON-RPC frames over a Unix-domain socket the app exposes inside its application data directory. Because the server lives inside the running app, scheduled meetings appear in Oatpad immediately and the toggle in Settings starts/stops the server in real time. If Oatpad isn't running (or its MCP toggle is off) the proxy reports a friendly error.",
  "author": {
    "name": "Tom Shelton-Lefley"
  },
  "icon": "icon.png",
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": [
        "${__dirname}/server/index.js"
      ]
    }
  },
  "tools": [
    {
      "name": "list_meetings",
      "description": "Search and list Oatpad meeting summaries with optional title query, date range, and limit. Newest first."
    },
    {
      "name": "get_meeting",
      "description": "Retrieve a single Oatpad meeting by id."
    },
    {
      "name": "get_meetings_in_range",
      "description": "Retrieve full meeting JSON for every Oatpad meeting whose effective time falls within an ISO 8601 datetime range."
    },
    {
      "name": "schedule_meeting",
      "description": "Create a new Oatpad meeting planned for a specific time, with a title and optional notetaker."
    }
  ]
}
