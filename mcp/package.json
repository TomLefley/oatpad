{
  "name": "oatpad-mcp",
  "private": true,
  "version": "1.0.1",
  "type": "module",
  "description": "Stdio↔Unix-socket proxy that bridges Claude Desktop to Oatpad's in-app MCP server",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack": "npm run build && npm prune --omit=dev && npx -y -p @anthropic-ai/mcpb@^2 mcpb pack . dist/oatpad.mcpb && npm install"
  },
  "dependencies": {},
  "devDependencies": {
    "@anthropic-ai/mcpb": "^2.1.2",
    "@types/node": "^25.8.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.6"
  }
}
